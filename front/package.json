{
  "name": "kanap-frontend",
  "version": "1.0.0",
  "description": "",
  "main": "server.js",
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "build": "esbuild server.js --bundle --platform=node --target=node24.11.1 --packages=external --outfile=server-build.js --format=esm",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^5.0"
  },
  "devDependencies": {
    "esbuild": "0.27.3"
  }
}
